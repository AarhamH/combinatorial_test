Run `python main.py`

Code was built with the assistances of AI tools (_Claude Code_) (particularly with `.csv` parsing and building command line args; so basically most of the work in `cmd_builder.py`). Construction of possible input values, fixtures, and oracle engineering was done manually by developers.
